# Use wildcard "*" to expose all endpoints.
# Expose individual endpoints with comma-delimited list.

spring.application.name=coolapp

management.endpoints.web.exposure.include=*
#management.endpoints.web.exposure.exclude=health
management.info.env.enabled=true

info.app.name=My App
info.app.description=This is a semo app for learning Sprint boot!
info.app.version=1.0.0

#
# Define custom properties.
#
coach.name=Micky Mouse
team.name=The Mouse Club

#
# Change Spring Boot embedded server port
#
server.port=7070

#
# Set the context path of the application.
#
# All requests should be prefixed with /mycoolapp
#
server.servlet.context-path=/mycoolapp